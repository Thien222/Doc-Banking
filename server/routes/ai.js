const express = require('express');
const router = express.Router();

// Placeholder route for AI functionality
router.get('/', (req, res) => {
  res.json({ message: 'AI route is working' });
});

module.exports = router; 